<?xml version="1.0" encoding="utf-8" ?>
<problem package="ru.ejudge.sample_contest" id="3" type="standard">
  <statement language="ru_RU">
    <title>Вариант &numero;3</title>

    <description><p>На вход подаётся пакет тестов, содержащий набор квадратных матриц. Каждую матрицу, поданную на вход, необходимо обработать согласно заданию: перестановка столбцов с максимальной и минимальной суммой элементов (с минимальными номерами соответственно).</p>
    </description>

    <input_format><p>В первой строке задаются два числа &mdash; количество <b>0 &le; t &le; 50</b> тестов и максимальный размер <b>n &ge; 0</b> матрицы, которая встретится среди тестов.</p>
    <p>Далее в первой строке каждого теста указан размер <b>m &le; n</b> матрицы, за которым следуют не превосходящие по модулю 45 тысяч целочисленные элементы матрицы.</p>
    </input_format>

    <output_format><p>Ответ (обработанная матрица) на каждый тест должен начинаться с новой строки. Гарантируется, что все результирующие значения помещаются в long long.</p>
    </output_format>
  </statement>

  <examples>
    <example>
      <input><pre>2 3
3
1 2 3
4 5 6
7 8 9
1
1000 </pre></input>
      <output><pre>3 2 1 
6 5 4 
9 8 7 
1000 
</pre>
      </output>
    </example>
  </examples>
</problem>
