<?xml version="1.0" encoding="utf-8" ?>
<problem package="ru.ejudge.sample_contest" id="18" type="standard">
  <statement language="ru_RU">
    <title>Вариант &numero;18</title>

    <description><p>На вход подаётся произвольный набор английских слов, разделённых пробелами, запятыми, знаками табуляции и границами строк. Букву 'y' для простоты считать согласной (гласными или согласными бывают звуки, но не буквы). Необходимо проверить выполнение следующего условия: есть ли два соседних слова с непересекающимся набором согласных? При решении задачи необходимо реализовать математическую абстракцию множества (сам тип, полное/пустое множество, пересечение, симметрическая разность и т.д.). Для реализации множества необходимо использовать битовые операции, но при этом запрещается нарушать принципы абстракции и инкапсуляции: пользовательский код должен зависеть только от интерфейса, но не от его реализации. При имплементации множеств можно считать, что все они имеют мощность не больше 32. Помимо обычных заголовочных файлов разрешается использовать ctype.h, inttypes.h и stdbool.h. Код функции main должен зависеть только от интерфейса АТД множество. Пояснения по тонким моментам: <a href="http://bit.ly/lab_13">http://bit.ly/lab_13</a>.</p>
    </description>

    <input_format><p>Слова во входной последовательности имеют произвольную длину и состоят только из строчных и прописных букв английского алфавита. Слова могут быть обрамлены любым количеством пробелов, запятых, знаков табуляции и переводов строк. Входной текст закодирован в произвольной кодировке, содержащей ASCII в качестве подмножества и допускающей ASCII-интерпретацию.</p>
    </input_format>

    <output_format><p>Вывести Yes, если результат проверки положительный, и No в противном случае. Как всегда, вывод должен завершаться Unix-переводом строки.</p>
    </output_format>
  </statement>

  <examples>
    <example>
      <input><pre>ipso facto</pre></input>
      <output>Yes</output>
    </example>
    <example>
      <input><pre>nota notae est nota</pre></input>
      <output>No</output>
    </example>
  </examples>
</problem>
